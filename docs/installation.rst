============
Installation
============

At the command line::

    $ easy_install django-genome

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-genome
    $ pip install django-genome
